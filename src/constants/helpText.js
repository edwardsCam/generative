const helpText = `
Select a pattern from the left tray.

Another tray will open on the right where you can tweak the parameters of the current pattern.
Sliders can be dragged left and right.

The app is fully keyboard navigable.
Use Tab and Shift+Tab to navigate between elements.
Hit Enter to press a button.
When focused on a slider, you can decrease/increase the values with the '[' and ']' keys.
While holding '[' or ']' down, you can speed up the decrement/increment rate by holding shift or ctrl.
         (shift): x5 speed
          (ctrl): x10 speed
  (shift + ctrl): x50 speed
`;

export default helpText;
